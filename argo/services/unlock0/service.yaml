apiVersion: v1
kind: Service
metadata:
  name: unlock0
  labels:
    app.kubernetes.io/name: unlock0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: unlock0
  ports:
    - name: http
      port: 80
      targetPort: http
